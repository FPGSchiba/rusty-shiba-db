name: Release

on:
  pull_request:
    types:
      - closed

jobs:
  build:
    runs-on: ubuntu-latest
    if: github.event.pull_request.merged
    steps:
      - uses: actions/checkout@v2
      - name: Set up Go
        uses: actions/setup-go@v4
        with:
          go-version: '1.22'

      - name: Generate build files
        uses: thatisuday/go-cross-build@v1
        with:
          platforms: 'linux/amd64, linux/arm64, darwin/amd64, darwin/arm64, windows/amd64, windows/arm64'
          package: 'rsdb/src'
          name: 'rusty-shiba-db'
          compress: 'true'
          dest: 'dist'

      - name: Tag
        id: tag
        uses: K-Phoen/semver-release-action@master
        with:
          release_branch: master
          release_strategy: tag
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Release with Notes
        uses: softprops/action-gh-release@v1
        with:
          files: |
            ./dist/rusty-shiba-db-linux-amd64.tar.gz
            ./dist/rusty-shiba-db-linux-arm64.tar.gz
            ./dist/rusty-shiba-db-darwin-amd64.tar.gz
            ./dist/rusty-shiba-db-darwin-arm64.tar.gz
            ./dist/rusty-shiba-db-windows-amd64.tar.gz
            ./dist/rusty-shiba-db-windows-arm64.tar.gz
          tag_name: ${{ steps.tag.tag }}
          generate_release_notes: 'true'
          draft: 'true'
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}